import time

print("I am sejal")

username = "sejal"
print(username)

# Iteration is the process of executing a block of code repeatedly
# In python iteration is achieved through for and while loop
# Iterable is an object that contains data and implement the __iter__() method. It produces an iterator when passed through iter()
# Iterator is the actual stateful object that fetches elements one by one using __next__() method.
# When no elements are left it raises a StopIteration exeception to gratefully terminate the loop.